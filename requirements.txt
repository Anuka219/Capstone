# Deployment requirements (Render / any host).
#
# The bot uses ElevenLabs (hosted) for voice, so the heavy local XTTS / torch /
# coqui stack is NOT needed to run it. This slim list installs fast and reliably
# on Render's free tier.
#
# Only app.py top-level imports + the document parsers used at startup:
#   httpx, dotenv, fastapi, pydantic  (top-level)
#   pypdf       -> reads the cached PDF knowledge docs at startup
#   python-pptx -> reads .pptx knowledge docs if any are added
#
# Want the offline local XTTS voice instead of ElevenLabs? Install the full
# stack from requirements-xtts.txt (large; needs ~2GB + torch).

fastapi==0.128.8
uvicorn==0.39.0
httpx==0.28.1
python-dotenv==1.2.1
pydantic==2.13.4
pypdf==6.13.2
python-pptx==1.0.2
